VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: the organizations.id row that represents Orin (the company) for
# its own subscription billing. The Billing page will default the platform-org
# dropdown to this id. If unset, the user picks an org manually (persisted to
# localStorage). See src/pages/Billing.jsx for the full TODO around this.
VITE_PLATFORM_ORG_ID=
